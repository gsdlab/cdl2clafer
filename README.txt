To run the code you will need Simple Build Tools.

To run the converter type the following in the command line:
sbt "run --convert FILE_NAME"
where FILE_NAME is relative/absolute path to the source IML model
